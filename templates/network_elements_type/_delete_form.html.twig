<form method="post" action="{{ path('network_element_type_delete', {'id': network_element_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ network_element_type.id) }}">
    <button class="btn">Delete</button>
</form>