{% extends 'base.html.twig' %}

{% block title %}NetworkElementsType{% endblock %}

{% block body %}
    <h1>NetworkElementsType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ network_elements_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ network_elements_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('network_elements_type_index') }}">back to list</a>

    <a href="{{ path('network_elements_type_edit', {'id': network_elements_type.id}) }}">edit</a>

    {{ include('network_elements_type/_delete_form.html.twig') }}
{% endblock %}