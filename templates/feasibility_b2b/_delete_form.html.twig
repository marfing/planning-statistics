
<form method="post" action="{{ path('feasibility_b2b_delete', {'id': feasibility_b2_b.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ feasibility_b2_b.id) }}">
    <button class="btn btn-danger mt-2">Delete</button>
</form>
